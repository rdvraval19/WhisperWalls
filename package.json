{
  "name": "my-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx expo start",                  // Start command for local development
    "android": "npx expo start --android",      // Android start command
    "ios": "npx expo start --ios",              // iOS start command
    "web": "npx expo start --web",              // Web start command for local dev
    "build:web": "npx expo build:web",          // Web build command for production (Vercel)
    "postinstall": "npx expo install"           // Ensure dependencies are installed correctly on Vercel
  },
  "dependencies": {
    "@react-native-async-storage/async-storage": "2.1.2",
    "@react-navigation/bottom-tabs": "^7.4.7",
    "@react-navigation/native": "^7.1.17",
    "@react-navigation/stack": "^7.4.8",
    "expo": "~53.0.22",
    "expo-linear-gradient": "~14.1.5",
    "expo-location": "~18.1.6",
    "expo-status-bar": "~2.2.3",
    "react": "19.0.0",
    "react-dom": "^18.0.0",  // Ensure react-dom is installed for web support
    "react-native": "0.79.6",
    "react-native-gesture-handler": "~2.24.0",
    "react-native-safe-area-context": "5.4.0",
    "react-native-screens": "~4.11.1"
  },
  "devDependencies": {
    "@babel/core": "^7.20.0"
  },
  "private": true
}
